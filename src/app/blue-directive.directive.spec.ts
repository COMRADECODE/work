import { BlueDirectiveDirective } from './blue-directive.directive';

describe('BlueDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
